describe("Get CSV Standardised Invoice", () => {
  // TODO Not needed if the type guard is in place in the handler
  test("should throw an error if the csvObject is not in the expected format", () => {});

  test("should build up the standardised invoice with a valid csvObject with one line item", () => {});

  test("should build up the standardised invoice with a valid csvObject with multiple line items", () => {});

  // TODO Is a test needed that will infer the sevice name based on the regex?
});
