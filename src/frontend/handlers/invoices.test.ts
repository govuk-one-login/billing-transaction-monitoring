import { getInvoicesHandler } from "./invoices";
// import { Request, Response } from "aws-ssm";
// import {render} from "sass";
//
// const mockedRequest = Request as jest.MockedClass<typeof Request>;
// const mockedResponse = Response as jest.MockedClass<typeof Response>;

// const mockedResponse.render()
describe("front end invoices handler", () => {
  // beforeEach(() => {
  //   request = {
  //     query: {
  //       contract_id: "a contract id"
  //     }
  //   };
  //
  // });
  //
  // test("getInvoicesHandler should retrieve config and use it to render page", () => {
  //   await getInvoicesHandler(request, mockedResponse);
  //   expect(response
  // })
});
