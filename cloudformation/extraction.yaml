ExtractFunction:
  # checkov:skip=CKV_AWS_117: VPC not needed for lambda
  # checkov:skip=CKV_AWS_116: DLQ not needed for lambda
  Type: AWS::Serverless::Function
  Properties:
    DeadLetterQueue:
      TargetArn: !GetAtt ExtractFunctionDeadLetterQueue.Arn
      Type: SQS
    Environment:
      Variables:
        TEXT_EXTRACT_ROLE: !GetAtt TextractRole.Arn # This is needed for the params for the textract call
        SNS_TOPIC: !Ref AmazonTextractRawInvoiceDataTopic
    Events:
      ExtractEvent:
        Type: SQS
        Properties:
          FunctionResponseTypes:
            - ReportBatchItemFailures
          Queue: !GetAtt ExtractQueue.Arn
    Handler: extract.handler
    KmsKeyArn: !GetAtt KmsKey.Arn
    Policies:
      - AWSLambdaBasicExecutionRole
      - Statement:
          - Effect: Allow
            Action:
              - sqs:DeleteMessage
              - sqs:GetQueueAttributes
              - sqs:ReceiveMessage
            Resource: !GetAtt ExtractQueue.Arn
          - Effect: Allow
            Action:
              - "textract:*" # To-Do What are the available textract actions and resources? Tighten this once working.
            Resource: "*"
          - Effect: Allow
            Action:
              - kms:Decrypt
              - kms:GenerateDataKey*
              - kms:Encrypt
            Resource: !GetAtt KmsKey.Arn
    ReservedConcurrentExecutions: 10

ExtractQueue:
  Type: AWS::SQS::Queue
  Properties:
    KmsMasterKeyId: !GetAtt KmsKey.Arn
    RedrivePolicy:
      deadLetterTargetArn: !GetAtt ExtractQueueDeadLetterQueue.Arn
      maxReceiveCount: 1

ExtractQueueDeadLetterQueue:
  Type: AWS::SQS::Queue
  Properties:
    KmsMasterKeyId: !GetAtt KmsKey.Arn

TextractRole:
  Type: AWS::IAM::Role
  Properties:
    AssumeRolePolicyDocument:
      Version: "2012-10-17"
      Statement:
        - Effect: "Allow"
          Principal:
            Service:
              - "textract.amazonaws.com"
          Action:
            - "sts:AssumeRole"
    Policies:
      - PolicyName: "TextractRoleAccess"
        PolicyDocument:
          Version: "2012-10-17"
          Statement: # To-Do Check if S3 access is needed.
            - Effect: Allow
              Action:
                - "sns:Publish"
              Resource: !Ref AmazonTextractRawInvoiceDataTopic

ExtractQueuePolicy:
  Type: AWS::SQS::QueuePolicy
  Properties:
    PolicyDocument:
      Statement:
        - Action: SQS:SendMessage
          Condition:
            ArnLike:
              aws:SourceArn: arn:aws:s3:::*
            StringEquals:
              aws:SourceAccount: !Ref AWS::AccountId
          Effect: Allow
          Principal: "*"
          Resource: "*"
    Queues:
      - !Ref ExtractQueue
